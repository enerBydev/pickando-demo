<?xml version="1.0" encoding="utf-8"?>
<!--
    Network security config — production-hardened.
    Only HTTPS is permitted (cleartextTrafficPermitted=false in manifest).
    We trust only system CA store (not user-installed CAs) to prevent
    MITM attacks via user-installed certificates.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    <!-- Allow cleartext only for localhost (dev/emulator debugging). -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
    </domain-config>
</network-security-config>
