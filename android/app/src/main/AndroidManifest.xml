<?xml version="1.0" encoding="utf-8"?>
<!--
    Nitheky AndroidManifest — hardened for production installs.

    Hardening checklist:
      - debuggable explicitly false (no reliance on build-type defaults)
      - extractNativeLibs=true so future NDK libs work without page-align step
      - supportsRtl=true for proper RTL layout
      - largeHeap=true (WebView is memory-hungry)
      - fullBackupContent + dataExtractionRules for Android 12+ compliance
      - usesCleartextTraffic=false (we only load HTTPS URLs)
      - explicit hardware acceleration
      - explicit requestLegacyExternalStorage
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.pickando.demo">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Declare hardware features as NOT required so the APK installs on devices
         without them (e.g. tablets without telephony). -->
    <uses-feature android:name="android.hardware.touchscreen" android:required="false" />

    <application
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"
        android:debuggable="false"
        android:supportsRtl="true"
        android:largeHeap="true"
        android:hardwareAccelerated="true"
        android:usesCleartextTraffic="false"
        android:networkSecurityConfig="@xml/network_security_config"
        android:extractNativeLibs="true"
        tools:targetApi="34">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|keyboardHidden|smallestScreenSize|screenLayout"
            android:screenOrientation="portrait"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
